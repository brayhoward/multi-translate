const light = '#fafafa';
const lightMedium = '#949494';
const medium = '#7a7a7a';
const medLight = '#393E42';
const medDark = '#303337';
const dark = '#222';

export const colors = {
  light,
  lightMedium,
  medium,
  medLight,
  medDark,
  dark
}

// For debugging purposes
export const bd = { borderWidth: 1, borderColor: 'red' }